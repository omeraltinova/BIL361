# OpenRouter API anahtarı (zorunlu)
OPENROUTER_API_KEY="your_openrouter_api_key"

# Varsayılan model ve üretim ayarları (opsiyonel)
# Tüm model isimleri: https://openrouter.ai/models
OPENROUTER_DEFAULT_MODEL="openai/gpt-oss-20b"
OPENROUTER_DEFAULT_ROWS=20
OPENROUTER_DEFAULT_RUNS=5

# Seed CSV yolu (opsiyonel)
# Örnek seed dosyası: Test/example_seed.csv
OPENROUTER_DEFAULT_SEED="Test/example_seed.csv"

# Sağlayıcı tercihleri (opsiyonel) — küçük harf slug kullanın (örn: azure, openai, togetherai)
# Sadece şu sağlayıcı(lar)ı kullan (only):
# OPENROUTER_PROVIDER_ONLY="azure"
# Şu sağlayıcı(lar)ı hariç tut (except):
# OPENROUTER_PROVIDER_EXCEPT="togetherai"

# Alternatif olarak order/allow/deny kombinasyonu:
# OPENROUTER_PROVIDER_ORDER="openai,azure"
# OPENROUTER_PROVIDER_ALLOW="openai,azure"
# OPENROUTER_PROVIDER_DENY="togetherai"

# Notlar:
# - Bu dosyayı .env olarak kopyalayın ve değerleri güncelleyin.
# - .env git'e dahil edilmez (.gitignore).
